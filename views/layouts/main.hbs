<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>

  <!-- Global styles that every page gets -->
  <link rel="stylesheet" href="/style.css" />

  <!-- (Optional) per-page styles go here -->
  {{{styles}}}
</head>
<body>
  {{> navbar}}

  <main class="container">
    {{{body}}}
  </main>

  {{> footer}}

  <!-- (Optional) per-page scripts go here -->
  {{{scripts}}}
</body>
</html>
